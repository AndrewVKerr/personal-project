package net.mcorp.utils.lockable;

/**
 * This object simply implements the {@linkplain LockableObject} class, the only reason to not just remove the abstract keyword from LockableObject
 * is that this class is intend to have more methods being added instead of being added at the LockableObject class.
 * @author Andrew Kerr
 */
public class LockObject extends LockableObject {}
